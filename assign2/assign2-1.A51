	ORG 4000H
	MOV PSW, #08H
;
	MOV A, #15H
	MOV DPTR, #3000H
	MOVX @DPTR, A
;
	MOV A, #28H
	MOV DPTR, #3001H
	MOVX @DPTR, A
;
	MOV A, #7AH
	MOV DPTR, #3002H
	MOVX @DPTR, A
;
	MOV A, #44H
	MOV DPTR, #3003H
	MOVX @DPTR, A
;
	MOV A, #28H
	MOV R0, #44H
	XRL A, R0
	MOV DPTR, #3005H
	MOVX @DPTR, A
;
	MOV A, #15H
	MOV R0, #7AH
	XRL A, R0
	MOV DPTR, #3004H
	MOVX @DPTR, A
;
	MOV PSW, #0H
	JMP 0057H
;
	END
	